{
  "name": "@conduit/client-app",
  "version": "0.1.2",
  "private": true,
  "description": "Conduit Client desktop app: bundles Tauri shell + Vue UI + Python core (smart local proxy)",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
