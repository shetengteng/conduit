{
  "name": "@conduit/client-app",
  "version": "0.2.3",
  "private": true,
  "description": "Conduit Client desktop app: pure-Rust Tauri shell + Vue UI (smart local proxy)",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
