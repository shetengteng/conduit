{
  "name": "@conduit/server-app",
  "version": "0.2.0",
  "private": true,
  "description": "Conduit Server desktop app: pure Rust Tauri shell + Vue UI",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
