# Conduit 验收指南（截至 M-δ / 完成度 ≈ 95%）

> **生成时间**:2026-05-02
> **范围**:本文档对应 server-app 完整功能 + client-app M-α / M-β.1 / M-β.2 / M-γ / **M-δ(应用功能部分)**。
> **新增于 M-δ**:独立诊断页(5 项 + 修复建议 + 复制报告)、状态托盘菜单(自动跟踪连接 + 一键断开)、macOS 开机自启 Switch。
> **未完成**:DMG 打包 / Apple notarization / 自动更新机制 —— 涉及 codesign 配置与 Apple ID,留 S4 与 server-app 一并处理。

---

## 0. TL;DR(60 秒上手)

```bash
# 终端 1:启动 server(广播 mDNS,提供 SOCKS5 / HTTP-CONNECT / PAC)
cd /Users/TerrellShe/Documents/personal/tt-projects/conduit
pnpm dev:server

# 终端 2:启动 client(自动发现同网段 server,UI 弹出)
pnpm dev:client
```

打开 client 窗口 → 「发现」页 → 点 server 卡片的「连接」 → 自动跳到「已连接」页 → 浏览任意网站 → 看流量曲线动 + 路由命中表实时增长。

---

## 1. 已交付功能清单

### Server-app(完整)

| 模块 | 能力 | 验收点 |
|---|---|---|
| 智能 SOCKS5 + HTTP-CONNECT 代理 | 监听 8080(HTTP)/8081(SOCKS5),响应 PAC,转发流量 | server 日志看到 `CONNECT xxx policy=vpn ... sent=N recv=N` |
| mDNS 广播 | `_conduit._tcp.local.`,TXT 含 name/port/socks/api/vpn/version/pac | client-app 自动出现卡片 |
| 控制 API(8090) | healthz / status / traffic / events SSE / admin | curl localhost:8090/healthz 200 |
| 自检与生命周期 | http_port / socks5_port / api_port / lan_ip / vpn 五项检查 | UI 仪表盘自检条 |
| 内置 Vue 控制台 | Stripe/Vercel B 风格,shadcn-vue,Tailwind v4 | 主窗口仪表盘 + 日志 + 设置 三页可用 |
| 23 个 pytest | 核心模块覆盖率 > 80% | `cd server-app/core && pytest -q` |

### Client-app

| 阶段 | 范围 | 验收点 |
|---|---|---|
| **M-α 骨架点亮** ✅ | Sidebar / TopStatusBar / boot phase / healthz 探活 | 启动后看到主界面 + 端口正确显示 |
| **M-β.1 真发现** ✅ | mDNS browser + known-servers.json 历史 + SSE 增量 | 「发现」页同网段 server 5–10 秒内自动出现 |
| **M-β.2 真连接** ✅ | 5 步连接进度(probe → fetch_pac → prefill_cache → switch_endpoint → start_heartbeat)+ 系统代理切换 + heartbeat | 点「连接」→ stepper 5 步绿勾 → 进 ConnectedView |
| **M-γ 流量与路由** ✅ | TrafficMeter + 1Hz traffic_tick SSE + route_decision SSE + /api/cache + /api/traffic + UI 双线图 + 缓存表 | ConnectedView 看见曲线动、缓存表行数增长、心跳胶囊绿色 |
| **M-δ 配套** ✅ | 独立诊断页(5 项检查 + 修复建议 + 一键复制报告)+ 状态托盘菜单(实时显示连接 + 跳转 + 断开)+ macOS 开机自启 Switch (LaunchAgent plist) | 「诊断」页 5 项绿勾、菜单栏图标右键看到当前已连接 server、设置页打开「开机自启」后下次登录自动拉起 |
| 116 个 pytest + 2 个 Rust 单测 | discoverer / connectivity / connect API / cache API / traffic_meter / route_decision / **diagnose API** / **autostart plist 渲染** 全覆盖 | `cd client-app/core && pytest -q` + `cd client-app/src-tauri && cargo test --lib` |

---

## 2. 一步步验收清单

### 2.1 启动准备

确认终端在项目根目录,Node 18+,Python 3.10+,macOS。检查 dev 模式 sidecar 行为:

| 环境变量 | 默认 | 说明 |
|---|---|---|
| `CONDUIT_NO_MDNS=1` | 未设置 | server 启动时跳过 mDNS 广播(只用于隔离调试) |
| `CONDUIT_NO_SYSTEM_PROXY=1` | 未设置 | client 连接成功后不切系统代理(只用于隔离调试) |

不设置任何变量 = 推荐的"完整体验"模式。

### 2.2 启动两个 sidecar(2 个终端)

**终端 1:server-app**
```bash
cd /Users/TerrellShe/Documents/personal/tt-projects/conduit
pnpm dev:server
```
会拉起 Tauri 主窗口 + Python sidecar。等到看到 `mDNS advertised: Conduit on ...` 表示 server 就绪。

**终端 2:client-app**
```bash
cd /Users/TerrellShe/Documents/personal/tt-projects/conduit
pnpm dev:client
```
拉起 client Tauri 窗口。第一次 macOS 会弹出 **本地网络访问权限**,**必须点允许**(否则 mDNS 收不到广播)。

### 2.3 「发现」页验收

1. 应在 5–10 秒内看到 server 卡片(name 为本机主机名)
2. 卡片右上"在线"绿点
3. 卡片显示 host:port、SOCKS / API 端口、VPN ✓、版本号、广播时间
4. 点「重新扫描」按钮无报错
5. 关掉 server-app(终端 1 Ctrl+C)→ 大约 1–2 分钟后卡片变灰显示「曾见过 X 分钟前」

> **如果发现页空着**:
> - 卡片永远不出来 → macOS 系统设置 → 隐私与安全性 → 本地网络 → 允许 client-app
> - 显示"自动发现未启用" → sidecar 未装 zeroconf,执行 `pip install zeroconf` 后重启 client

### 2.4 「连接」流程验收

1. 重启 server(终端 1 重新跑 `pnpm dev:server`),client 「发现」页 server 卡片重新变绿
2. 点卡片上的「连接」按钮
3. 自动跳转到「已连接」标签页
4. 看到 5 步竖向 stepper:
   - ① 可达性检查 → 绿勾,detail "延迟 N ms"
   - ② 拉取 PAC → 绿勾,detail "N 字节"
   - ③ 解析 PAC 预填路由 → 绿勾,detail "预填 N 条规则"
   - ④ 切换上游 server → 绿勾,detail "上游 IP:port · 本机 SOCKS5 :NNNN · 系统代理 已切换"
   - ⑤ 启动心跳与系统代理 → 绿勾,detail "心跳每 10 秒一次 · 流量统计已启用"
5. 5 步全绿后自动切到 ConnectedView,显示:
   - 顶部"已连接到 <server name>" + 右上"断开连接"按钮
   - 主信息卡:名称 / IP / 心跳胶囊(绿色"健康") / 已连接时长(实时 ticking)/ SOCKS / API / VPN
   - 流量曲线卡:上行(绿)+ 下行(蓝)双线 + 实时速率 + 累计字节
   - 路由命中卡:host / 方向 / 来源 / 命中数 / 最近使用
6. Sidebar 「已连接」标签出现绿色状态点

### 2.5 真实流量验收(同时验证 M-γ 的核心闭环)

打开任意浏览器,**不需要手动配代理**(连接时已自动切系统代理),正常访问:

| 站点 | 期望方向 | 期望来源 | 在 ConnectedView 路由命中表的表现 |
|---|---|---|---|
| https://www.baidu.com | direct | probe(本地 TCP 探测可通) | 第一次显示 direct/TCP 探测 |
| https://www.google.com | proxy | pattern(PAC 命中) | 显示 proxy/通配符 |
| https://github.com | proxy | pattern | 显示 proxy/通配符 |
| https://example.com | direct(海外但可直连) | probe | 显示 direct/TCP 探测 |

**同时观察**:
- 流量曲线立即出现尖峰
- 上行/下行实时速率刷新
- 累计字节单调增长
- 路由命中表行数增长

**如果不通过浏览器,而想验证后端**:
```bash
# 看 client 后端 API
curl -fsS http://127.0.0.1:<client_api_port>/api/connection
curl -fsS http://127.0.0.1:<client_api_port>/api/cache | head -50
curl -fsS http://127.0.0.1:<client_api_port>/api/traffic
# <client_api_port> 在 client 设置页可见(或 client sidecar 启动日志 "control API listening on 127.0.0.1:NNNN")

# 走 SOCKS5 测试(替换 NNNN 为 client SOCKS5 端口)
curl -sS --socks5-hostname 127.0.0.1:NNNN https://www.google.com -o /dev/null -w '%{http_code}\n'
```

### 2.6 「断开」验收

1. 在「已连接」页点右上"断开连接"
2. 状态切回 idle,卡片消失,提示"尚未连接"
3. ConnectedView 显示"去发现页"按钮
4. Sidebar 状态点消失
5. 此时浏览器访问 google.com 应该:**断了**(因为系统代理已恢复但 client 不再有上游)
6. 重新连接后自动恢复

### 2.7 「设置」页验收

进入 Sidebar 「设置」:

1. **运行时**:看到 SOCKS5 + Control API 端口
2. **开机自启**:打开 Switch → toast 提示"已启用",检查 `~/Library/LaunchAgents/com.conduit.client.plist` 已生成,关掉 Switch → plist 应被删掉
3. **手动连接 server**:输入 host/port 等可点「尝试连接」(用于 mDNS 不通的兜底,只能连之前发现过的)
4. **缓存维护**:显示当前缓存条目数 + 命中/未命中,可一键清空
5. **自检详情**:点「打开诊断」按钮跳转到独立的诊断页(下一节验)
6. **关于**:显示版本

> **开机自启限制**:dev 模式(`pnpm dev:client`)直接跑 cargo,不存在 .app bundle,Switch 切到开会 toast 报"未找到 .app bundle";打包后再打开自启验。

### 2.8 「诊断」页验收(M-δ 新增)

进入 Sidebar 「诊断」:

1. 进页面自动跑一次 5 项检查,顶部状态卡显示"一切正常"或"X 项需要关注"
2. 5 项依次为:
   - **Sidecar 进程**:PID + SOCKS5/Control 端口 + 已运行 N 秒
   - **mDNS 服务发现**:在线 N 个 / 历史 N 个;若 zeroconf 未装会标 FAIL 并给安装命令
   - **上游 Server 可达**:HTTP+SOCKS5 双端口实时探测;未连接则跳过
   - **PAC 文件**:实际拉一次 PAC URL,显示字节数 + 已预填规则数;未连接则跳过
   - **系统代理**:已连接且 enable_system_proxy 为 true 时校验是否真的 active
3. 任何 FAIL 项下方应有琥珀色 remediation 框,文字为多行操作建议
4. 右上「重新检测」可重跑;「复制报告」复制成多行 plain text(贴 issue / 群里方便排错)

> **故意制造失败验证**:
> - 关掉 server-app → 重新检测 → "上游 Server 可达" 应该 FAIL,detail "双端口都不可达"
> - 启动 client 时加 `CONDUIT_NO_MDNS=1` → "mDNS 服务发现" 仍 OK 但 "在线 0 个,历史 N 个"

### 2.9 系统托盘菜单验收(M-δ 新增)

打开 Tauri 客户端后,看 macOS 菜单栏应该有 Conduit 图标。点开:

1. 第 1 行(灰色不可点):**当前状态**,文字会 5 秒一次自动刷新
   - 未连接:"状态: 未连接"
   - 连接中:"状态: 正在连接…"
   - 已连接:"已连接: <name> (<host>:<port>)" / 心跳异常时尾巴加 "· 波动" 或 "· 失联"
   - 失败:"状态: 上次连接失败"
2. **打开主窗口**:把窗口拉到前台
3. **打开诊断页** / **打开设置页**:打开窗口并自动切到对应 NavKey
4. **断开连接**:已连接时可用,点击直接 POST `/api/disconnect`,无需打开主窗口
5. **退出 Conduit Client**:同主窗口的退出

> **托盘左键单击**:直接打开主窗口(等同点 "打开主窗口")

### 2.10 失败与降级路径验收

| 场景 | 操作 | 期望 |
|---|---|---|
| 连接中 server 突然挂 | 连接成功后,在终端 1 Ctrl+C 杀掉 server | 心跳胶囊 1 分钟内变黄/红;曲线归零;UI 显示"与 server 失联"警告 |
| 重连同一个 server | server 杀掉再重启 | mDNS 重新广播,卡片重新变绿;手动点连接重新走 5 步 |
| 连接到不存在的 server | 在设置页输入错误 host:port | toast 报"NOT_FOUND",状态保持 idle,无脏副作用 |
| connect 第 1 步失败(server 关机) | server 关机后立即点连接 | stepper 第 1 步红叉,显示具体错误,系统代理保持原状 |
| 二次连接同一 server | 已 connected 状态再点连接 | 后端返回幂等成功,状态不变 |
| 同时点连接两次 | 快速双击连接按钮 | 第二次返回 BUSY,UI 不会进入混乱状态 |

---

## 3. API 速查(给开发/调试用)

| 端点 | 方法 | 用途 |
|---|---|---|
| `/healthz` | GET | sidecar 探活 |
| `/api/servers` | GET | 当前发现的 server 列表(mdns + history) |
| `/api/connection` | GET | 当前连接快照 |
| `/api/connect/{server_id}` | POST | 连接(server_id = `name@host:port`) |
| `/api/disconnect` | POST | 断开 |
| `/api/cache` | GET | 路由缓存表(可加 `?direction=` `?source=` `?limit=`) |
| `/api/cache` | DELETE | 清空路由缓存 |
| `/api/traffic` | GET | 流量快照(总累计 + 当前秒) |
| `/api/diagnose` | GET | 5 步自检(sidecar/mDNS/server_reach/PAC/system_proxy) |
| `/api/events` | GET (SSE) | 实时事件流 |

**SSE 事件类型清单**:
- `ready` —— sidecar 启动完成
- `server_discovered` / `server_lost` —— mDNS 出现 / 消失
- `connection_state_changed` —— state ∈ {idle, connecting, connected, disconnecting, failed}
- `connect_progress` —— 5 步进度 ({step, total, key, label, status, detail, server_id})
- `connect_done` —— 与 connection_snapshot 同结构
- `heartbeat_changed` —— tone ∈ {green, yellow, red}
- `traffic_tick` —— 1Hz {ts, uplink_bytes, downlink_bytes, total_uplink, total_downlink}
- `route_decision` —— 每次决策 {host, port, direction, source, hit_count}

---

## 4. 故障排查

### Q1. 「发现」页一直空着,看不到任何 server

| 原因 | 解决 |
|---|---|
| macOS 「本地网络访问」未授权 | 设置 → 隐私与安全性 → 本地网络 → 勾上 client-app |
| zeroconf 模块未装 | `pip install zeroconf`,重启 client |
| 与 server 不在同网段 | mDNS 不跨网段;改用「设置」页手动连接 |
| 公司 WLAN 禁用 multicast | 同上;改用手动连接,或要 IT 解禁 |
| server 没起 mDNS | 检查 server 启动日志有没有 `mDNS advertised`;CONDUIT_NO_MDNS 环境变量是否被设了 |

### Q2. 连接 5 步卡在第 1 步(可达性检查)

server 端口被防火墙挡。直接 telnet 验证:

```bash
nc -zv <server_host> <server_socks_port>   # 应当 succeeded
nc -zv <server_host> <server_http_port>    # 应当 succeeded
```

### Q3. 连接成功但浏览器打不开网站

按顺序排查:

1. 是否系统代理切了?`networksetup -getsocksfirewallproxy Wi-Fi` 应该显示 `Yes`,`127.0.0.1:NNNN`
   - 如果是 `No`:client 启动时被加了 `CONDUIT_NO_SYSTEM_PROXY=1` 或 `--no-system-proxy`,需自己在系统设置里手动配 SOCKS5
2. SOCKS5 端口对吗?设置页可见
3. 直接走 curl 验:`curl -sS --socks5-hostname 127.0.0.1:NNNN https://www.google.com -o /dev/null -w '%{http_code}\n'`
   - 200 → 后端 OK,问题在浏览器/系统代理
   - 502/timeout → server 上游问题,看 server 日志

### Q4. 流量曲线一直是 0

只有真的过 client 的字节会被统计。如果浏览器请求都被 macOS 之前的代理或浏览器自身代理截走了,client 看不见。验法:用上一节 Q3 的 curl 命令直接走 SOCKS5,然后看曲线。

### Q5. 连接成功后断开,系统代理没自动还原

正常情况会自动 disable。如果没还原(异常崩溃),手动:

```bash
networksetup -setsocksfirewallproxystate Wi-Fi off
# 或在 macOS 设置 → 网络 → Wi-Fi → 详细信息 → 代理 → 关 SOCKS
```

下次启动 client 时 sidecar 也会探测并自动清理"指向自己的"残留代理(`cleanup_if_pointing_to_us`)。

### Q6. UI 进入了诡异状态,想从头开始

```bash
# 杀干净所有 sidecar
pkill -f proxy_server.py
pkill -f client_main.py
# 清掉 known-servers 历史
rm ~/Library/Application\ Support/Conduit/known-servers.json
# 重启
pnpm dev:server &
pnpm dev:client &
```

---

## 5. 测试覆盖

```bash
# server 后端
cd server-app/core && python3 -m pytest -q
# 期望:23 passed

# client 后端 (含 5 个 diagnose API 集成测试 / M-δ 新增)
cd client-app/core && python3 -m pytest -q
# 期望:116 passed

# client Tauri 主进程 (含 2 个 autostart plist 渲染单元 / M-δ 新增)
cd client-app/src-tauri && cargo test --lib
# 期望:2 passed

# server 前端
cd server-app/ui && pnpm vue-tsc --noEmit -p tsconfig.json && pnpm build
# 期望:vue-tsc 0 错,vite build 成功

# client 前端
cd client-app/ui && pnpm vue-tsc --noEmit -p tsconfig.json && pnpm build
# 期望:同上
```

---

## 6. 已知限制 / 待办

**S4 阶段(打包发布)处理**:
1. **DMG 打包 + Apple notarization**:目前只能 dev 模式 `pnpm dev:client` 启;打包 DMG 需要 Apple Developer ID + codesign 配置 + altool/notarytool 公证流程,与 server-app 一并在 S4 处理
2. **自动更新机制**:tauri-plugin-updater 集成留 S4
3. **沙箱(Hardened Runtime)配置**:打包时必须开,可能影响 mDNS multicast / system proxy 写入,留 S4 实测调整 entitlements

**S7 / 后续版本处理**:
4. **手动添加未发现的 server**:Settings 表单目前只能复连发现过的,完整的"输入信息直接注册"端点要后端加 `POST /api/servers`
5. **流量统计粒度**:目前只统计已建立的 SOCKS5 连接;失败连接 / DNS / handshake 时间未单独计量
6. **路由学习反馈**:前端目前只读路由决策,不能让用户手动覆盖某个 host 走 direct/proxy(后续加 PUT /api/cache/{host})
7. **i18n**:全部中文 hardcode,英文版排在 v0.2
8. **跨网段 / 公网穿透**:mDNS 限制单 LAN,非同网段需手动添加;后续考虑可选的 STUN / 中转
9. **Windows / Linux 客户端**:client-app v0.1 仅 macOS,Windows / Linux 推到 v0.2(autostart 的 plist 实现要换 Registry / systemd unit)

---

## 7. 文件结构速查

```
conduit/
├── server-app/                       # Server (macOS 智能代理 + 控制台)
│   ├── core/                         # Python sidecar
│   ├── src-tauri/                    # Rust 主进程
│   └── ui/                           # Vue 控制台 (B 风格 + shadcn-vue)
├── client-app/                       # Client (macOS 智能本地代理)
│   ├── core/                         # Python sidecar
│   │   ├── client_main.py            # 启动入口 + 5 步连接状态机 + _started_at uptime
│   │   ├── connectivity.py           # probe + Heartbeat (M-β.2)
│   │   ├── discoverer.py             # mDNS 监听 + known-servers (M-β.1)
│   │   ├── traffic_meter.py          # 流量计量 (M-γ)
│   │   ├── route_resolver.py         # direct/proxy 决策 + route_decision 事件 (M-γ)
│   │   ├── route_cache.py            # LRU + TTL cache
│   │   ├── local_proxy.py            # SOCKS5 监听 + 透传 + relay
│   │   ├── events_bus.py             # 进程内 pub/sub
│   │   └── api/                      # 控制 HTTP API + SSE
│   │       ├── server.py             # builder + middlewares
│   │       ├── healthz.py            # M-α
│   │       ├── discovery.py          # M-β.1
│   │       ├── events.py             # M-β.1 SSE
│   │       ├── connect.py            # M-β.2
│   │       ├── cache.py              # M-γ (含 traffic snapshot)
│   │       └── diagnose.py           # M-δ (5 步检查 + remediation)
│   ├── src-tauri/                    # Rust 主进程
│   │   └── src/
│   │       ├── lib.rs                # boot sequence + run loop
│   │       ├── sidecar.rs            # python3 spawn / kill
│   │       ├── tray.rs               # 状态托盘菜单 + 5s 轮询 (M-δ)
│   │       ├── autostart.rs          # macOS LaunchAgent plist (M-δ)
│   │       ├── commands.rs           # invoke handlers (含 autostart_*)
│   │       ├── healthz.rs            # client healthz wait
│   │       ├── state.rs              # AppRuntime + LifecyclePhase
│   │       └── error.rs              # ConduitError → JSON envelope
│   └── ui/                           # Vue 控制台
│       ├── src/types/client.ts       # 与后端严格对齐的类型(含 DiagnoseResponse)
│       ├── src/api/                  # client / runtime / client-api(含 diagnose())
│       ├── src/composables/          # useEvents / useDiscovery / useToast
│       ├── src/stores/               # ui(diagnose NavKey) / clientStore / discoveryStore / connectionStore / trafficStore / cacheStore
│       ├── src/components/business/  # ConnectingProgress / TrafficChart / CacheTable / DiagnoseRow
│       ├── src/components/layout/    # Sidebar(诊断菜单项) / TopStatusBar / Boot*
│       └── src/views/                # DiscoveryView / ConnectedView / DiagnoseView / SettingsView(开机自启 Switch)
└── design/                           # 设计文档 + TODO 跟踪
    ├── 2026-04-30-2-Conduit-Tauri+Python方案详细设计.md
    ├── 2026-04-30-3-Conduit-Client-客户端可行性报告.md
    ├── 2026-04-30-5-Conduit-开发TODO清单-进度S6Md-95.md
    └── 2026-05-02-1-Conduit-验收指南.md   ← 本文档
```
